services:
  - type: web
    name: trading-dashboard
    env: python
    plan: free
    buildCommand: bash build.sh
    startCommand: python backend/main.py
    envVars:
      - key: PORT
        value: "8000"
